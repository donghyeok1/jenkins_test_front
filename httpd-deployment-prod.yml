apiVersion: apps/v1
kind: Deployment
metadata:
 name: httpd-deployment-dev
 namespace: cdh-dev
spec:
 selector:
   matchLabels:
    type: front
 replicas: 1
 template:
   metadata:
     labels:
       type: front
   spec:
    containers:
    - name: container
      image: cis07385/front:latest
      envFrom:
      - configMapRef:
          name: frontend-cm
      ports:
      - containerPort: 80